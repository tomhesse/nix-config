{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "timezone": "Europe/Berlin",
  "assignees": ["tomhesse"],
  "enabledManagers": ["nix", "github-actions", "custom.regex"],
  "nix": {
    "enabled": true
  },
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 6am on sunday"],
    "commitMessageAction": "update the flake inputs",
    "semanticCommitScope": "flake"
  },
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/^modules/.*\\.nix$/"],
      "matchStrings": [
        "\"(?<depName>[a-z0-9-]+(?:\\.[a-z0-9-]+)+/[a-zA-Z0-9._/-]+):(?<currentValue>[a-zA-Z0-9._-]+)\""
      ],
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "matchManagers": ["github-actions"],
      "groupName": "the github actions",
      "schedule": ["before 6am on sunday"],
      "semanticCommitType": "ci",
      "semanticCommitScope": null
    },
    {
      "matchManagers": ["custom.regex"],
      "dependencyDashboardApproval": true,
      "semanticCommitScope": "{{{replace '\\.nix$' '' (replace '^modules/' '' packageFile)}}}"
    },
    {
      "matchPackageNames": ["docker.io/apache/tika"],
      "allowedVersions": "<4"
    },
    {
      "matchPackageNames": ["lscr.io/linuxserver/**"],
      "versioning": "regex:^v?(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)(\\.(?<build>\\d+))?-ls(?<revision>\\d+)$"
    }
  ],
  "semanticCommits": "enabled"
}
